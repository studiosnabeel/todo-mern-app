import React from 'react';

const TodoForm = () => {
  return (
    <form className="form-container">
      <input className="form-input" type="text" placeholder="Add tasks" />
    </form>
  );
};

export default TodoForm;
